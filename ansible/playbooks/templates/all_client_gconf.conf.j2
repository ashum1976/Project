# Загружаемые модули для работы rsyslog-a.

module(load="imudp")
module(load="imfile")

#Templates for clients. Шаблоны для формирования строки содержащей tag, дату, имя программы, важность, категорию и т.д.

template (name="LongTagForwardFormat" type="string" string="<%PRI%>%TIMESTAMP:::date-rfc3339% %HOSTNAME% %syslogtag%%$.suffix%%msg:::sp-if-no-1st-sp%%msg%")

#######
